# api local config
spring:
  config:
    activate:
      on-profile: local
  messages:
    basename: messages/messages
    encoding: UTF-8
  datasource:
   url: jdbc:mysql://localhost:3306/kickalert
   username: root
   password: 1234
   driver-class-name: com.mysql.cj.jdbc.Driver

#  datasource:
#    url: jdbc:mysql://libl-aurora-mysql8-db-dev.cluster-cshrhqgio6er.ap-northeast-2.rds.amazonaws.com/libl
#    username: admin
#    password: Libl!23$0
#    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    properties:
      hibernate:
        show_sql: true
        format_sql: true
        default_batch_fetch_size: 1000
    open-in-view: false

  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB

server:
  port: 8080

# logging 설정
logging:
  config: classpath:logback/logback-${spring.profiles.active}.xml
